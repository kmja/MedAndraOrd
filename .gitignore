node_modules/
web/dist/
.env
*.log

# The FileStore's JSON database at the repo root — anchored with a leading
# slash so it does not also swallow server/data/, which holds the committed
# word list.
/data/
